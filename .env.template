# Orchestrator's endpoint: use http://localhost:7071/api/orc when running everything locally.
ORCHESTRATOR_ENDPOINT="Add the local endpoint for orchestrator. Ex: http://localhost:7071/api/orc"
AZURE_ORCHESTRATOR_FUNC_NAME="Add the Azure Function name implementing the orchestrator"

# Azure configuration
AZURE_SUBSCRIPTION_ID="Add your Azure subscription ID"
AZURE_RESOURCE_GROUP_NAME="Add the resource group name hosting related resources"

# Speech synthesis and recognition settings
SPEECH_REGION="Add the Azure region where the Speech Service is deployed. Ex: eastus"
SPEECH_RECOGNITION_LANGUAGE="Add the speech-to-text recognition language. Ex: es-ES"
SPEECH_SYNTHESIS_LANGUAGE="Add the text-to-speech synthesis language. Ex: es-MX"
SPEECH_SYNTHESIS_VOICE_NAME="Add the neural voice name for speech synthesis. Ex: es-MX-BeatrizNeural"

# Document storage settings
STORAGE_ACCOUNT="Add the Azure Storage account name for document storage"

# Logging configuration
LOGLEVEL="Add logging level. Ex: DEBUG, INFO, WARNING, ERROR"

# MSAL / OIDC configuration for custom authentication
ENABLE_AUTHENTICATION="Set to true or fale to enable or disable authentication"
CLIENT_ID="Add the Azure AD application client ID"
AUTHORITY="Add the Azure AD authority URL. Ex: https://login.microsoftonline.com/your_tenant_id"
REDIRECT_PATH="Add redirect path. Ex: /getAToken. Must match the Azure AD app registration"
FLASK_SECRET_KEY="Add a secret key for Flask"	
FORWARD_ACCESS_TOKEN_TO_ORCHESTRATOR="Set to True or False to forward the access token to orchestrator"
AZURE_KEY_VAULT_NAME="Add the Azure Key Vault name"

# Authorization settings
ALLOWED_GROUP_NAMES="Comma-separated list of allowed Azure AD group names. Ex: group1,group2"
ALLOWED_USER_PRINCIPALS="Comma-separated list of allowed user principal names (UPNs). Ex: user1@domain.com,user2@domain.com"
ALLOWED_USER_NAMES="Comma-separated list of allowed usernames for application access. Ex: user1,user2"